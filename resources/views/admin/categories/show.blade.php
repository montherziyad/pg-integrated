<x-app-layout><x-slot name="header">Category Details</x-slot><div class="space-y-6">
<div class="flex justify-between"><div><h2 class="pg-title">{{ $category->name }}</h2><p class="pg-subtitle">{{ $category->code }}</p></div><div class="flex gap-3"><a href="{{ route('admin.categories.edit', $category) }}" class="pg-btn-primary">Edit</a><a href="{{ route('admin.categories.index') }}" class="pg-btn-secondary">Back</a></div></div>
<div class="pg-card"><div class="pg-card-body grid grid-cols-1 md:grid-cols-2 gap-6"><div><div class="pg-stat-label">Description</div><div class="font-semibold">{{ $category->description ?? '-' }}</div></div><div><div class="pg-stat-label">Status</div><div class="font-semibold">{{ $category->is_active ? 'Active' : 'Inactive' }}</div></div></div></div>
<div class="pg-card"><div class="pg-card-body overflow-x-auto"><h3 class="text-lg font-bold mb-4">Category Jobs</h3><table class="w-full text-sm"><thead><tr class="border-b text-left text-slate-500"><th class="py-3">Job</th><th>Client</th><th>Stage</th><th>Priority</th></tr></thead><tbody>@forelse($category->jobs as $job)<tr class="border-b last:border-b-0"><td class="py-3"><a class="font-semibold" href="{{ route('jobs.show', $job) }}">{{ $job->job_number }} — {{ $job->title }}</a></td><td>{{ $job->client?->name ?? '-' }}</td><td>{{ $job->currentWorkflowStage?->name ?? '-' }}</td><td>{{ $job->priority }}</td></tr>@empty<tr><td colspan="4" class="py-6 text-center text-slate-500">No jobs in this category.</td></tr>@endforelse</tbody></table></div></div>
</div></x-app-layout>
