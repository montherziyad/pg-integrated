<x-app-layout><x-slot name="header">{{ $ticket->ticket_number }}</x-slot>
<div class="grid lg:grid-cols-3 gap-6"><div class="lg:col-span-2 bg-white rounded-xl shadow p-6"><h2 class="text-2xl font-bold">{{ $ticket->subject }}</h2><p class="text-slate-500">{{ $ticket->status }} · {{ $ticket->priority }} · {{ $ticket->channel }}</p><div class="mt-6 space-y-4">@foreach($ticket->messages as $message)<div class="rounded-lg bg-slate-50 p-4"><div class="text-xs text-slate-500">{{ $message->sender_type }} · {{ $message->created_at->format('Y-m-d H:i') }}</div><p>{{ $message->message }}</p></div>@endforeach</div></div>
<form method="POST" action="{{ route('support.reply',$ticket) }}" class="bg-white rounded-xl shadow p-6 h-fit space-y-3">@csrf<h3 class="font-bold">Reply</h3><select name="sender_type" class="w-full rounded border-slate-300"><option>agent</option><option>customer</option><option>ai</option></select><textarea name="message" rows="8" class="w-full rounded border-slate-300"></textarea><button class="px-4 py-2 bg-slate-900 text-white rounded">Send</button></form></div>
</x-app-layout>
